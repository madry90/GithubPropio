<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.musical_structure_app">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PlayerActivity"
            android:label="Details of Songs"
            android:parentActivityName=".MainActivity"/>
        <activity
            android:name=".DanceActivity"
            android:label="@string/dance_music" />
        <activity
            android:name=".IndieActivity"
            android:label="@string/indie_music" />
        <activity
            android:name=".SoundtracksActivity"
            android:label="@string/soundtrack_music" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>